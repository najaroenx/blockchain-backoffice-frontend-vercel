version: '3.8'

services:
  frontend:
    build:
      context: .
      dockerfile: Dockerfile
    
    container_name: blockchain-backoffice-frontend
    
    ports:
      - "3000:3000"
    
    environment:
      - NODE_ENV=production
      - PORT=3000
      - NEXT_PUBLIC_API_URL=https://dlp-backoffice-testnet.ad!digitalservice.com
      - MERCHANT_BACKEND_URL=https://dlp-backoffice-testnet.ad!digitalservice.com
      - NEXT_PUBLIC_USE_EMULATOR=false
      - NEXTAUTH_SECRET=your-secret-key-here
      - NEXTAUTH_URL=http://localhost:3000
    restart: unless-stopped
    
    healthcheck:
      test: ["CMD", "wget", "--quiet", "--tries=1", "--spider", "http://localhost:3000"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s
    
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"
    
    networks:
      - blockchain-network

networks:
  blockchain-network:
    driver: bridge
